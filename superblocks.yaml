version: 1
jobs:
  build:
    image: node
    script:
    - npm i
    - npm run build
  test:
    image: node
    script:
    - npm i
    - npm run test

stages:
  - build:
      jobs:
        - test
  - test:
      jobs:
        - test
